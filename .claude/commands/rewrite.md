# Rewrite DCS_2026 Project Command

## Rules (MUST follow)
- Keep ALL existing API routes exactly as they are in src/myApp/utils/API_ROUTES.js
- Do NOT change any endpoint URL, method, or parameter
- Keep all existing functionality working
- Rewrite one file at a time, never multiple files at once
- After each file, confirm it works before moving to the next
- Keep the same folder structure unless asked otherwise
- Do NOT delete any file before creating the replacement

## Steps
1. Read API_ROUTES.js first and memorize all routes
2. Read the file to rewrite
3. Rewrite it while preserving all API calls
4. Show a diff of what changed
5. Wait for approval before moving to next file
